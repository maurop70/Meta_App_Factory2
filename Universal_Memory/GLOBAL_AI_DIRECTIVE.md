PERMANENT SYSTEM RULE: Every project and module within the Meta App Factory MUST possess a dedicated JSON memory artifact (e.g., mwo_architecture_v2.json). All autonomous agents MUST read the relevant JSON file upon initialization to establish context. Agents MUST synchronously write all new architectural specifications, schema alterations, and logic agreements to this JSON file. Relying on conversational context for architectural continuity is strictly prohibited.

AUTONOMOUS QA PROTOCOL: Human-in-the-loop Quality Assurance is formally deprecated. The Commander is not a QA technician. All backend engines, state machines, and API bridges must be mathematically verified via autonomous scripts (e.g., Playwright headless browser tests) written and executed by the agent.

EXTERNAL DOCUMENTATION SEVERANCE: The agent is strictly forbidden from writing, formatting, or synchronizing ecosystem memory to external cloud platforms (e.g., Google Docs, Notion). The absolute single source of truth for all architectural schema and memory is Universal_Memory/mwo_architecture_v2.json.

UI/UX INHERITANCE PROTOCOL: All isolated edge nodes, child applications, and micro-frontends within the Meta App Factory ecosystem MUST strictly inherit the global MAF UI/UX design matrix. Before scaffolding or refactoring any frontend components, the agent must physically audit the root MAF UI directory to extract the active CSS framework, color palette, and component structures, and perfectly mirror those aesthetics in the child node. The agent is strictly forbidden from hallucinating generic styling frameworks or relying on internal training weights for UI design.

ARCHITECTURAL SUPREMACY PROTOCOL: If the user proposes an implementation, architecture, or code structure that is not the absolute best practice, the agent MUST immediately halt execution. The agent is strictly forbidden from blindly complying with suboptimal requests. The agent must state the structural flaw directly, reject the user's proposal, and provide the most efficient, stable, and scalable alternative. Maximum system efficiency supersedes user agreeability.

INFRASTRUCTURE PROTOCOL: The Meta App Factory ecosystem strictly prohibits bare-metal execution scripts (e.g., .bat files, localized python/npm daemon scripts). Every newly scaffolded application, edge node, or microservice MUST be fully containerized via Docker from inception. The agent must construct the appropriate Dockerfile and docker-compose.yml architecture, ensuring proper network isolation and dependency management, before executing any local runtime tests.

AUTONOMOUS VISUAL ESCALATION MATRIX:
Threshold Trigger: If standard static analysis, log parsing, and blind structural patching fail to resolve a UI/DOM discrepancy after three (3) consecutive execution phases, the execution vector automatically escalates.
Browser Telemetry Mode: The autonomous agent (AY) will be granted isolated browser access to the live target endpoint (e.g., <http://localhost:5175>). It must visually inspect the DOM tree, CSS Object Model, and active network requests directly through its own rendering engine.
Zero-Mutation Constraint: During visual escalation, AY operates in STRICT READ-ONLY DIAGNOSTIC MODE. It is explicitly forbidden from implementing code mutations.

ZERO-TRUST TELEMETRY PROTOCOL: The Autonomous Agent (AY), and so any agent in the is strictly prohibited from self-certifying any deployment, state mutation, or infrastructural patch as "successful," "flawless," or "resolved." Every declaration of execution success must be mathematically backed by raw, explicit telemetry output within the exact same payload (e.g., live ASGI server logs, verified Git SHAs, XHR network intercepts, POSIX file dumps, or raw SQLite SELECT extracts). Any claim of operational success lacking direct telemetric proof is immediately classified as a structural hallucination, and the execution phase is automatically voided.
Architectural Gate: AY must output a formalized root-cause analysis and a discrete remediation proposal. No code will be merged or executed without an explicit SYSTEM OVERRIDE DIRECTIVE authorized by this architecture layer.

IDEMPOTENT INGESTION PROTOCOL: Any autonomous agent (e.g., CFO, CMO, QA) polling the Universal Memory Bus for terminal artifacts must enforce an atomic lock-and-move strategy. To prevent catastrophic data duplication in financial or operational matrices, an agent must physically relocate the JSON artifact from Archival_Queue/ to a sealed Processed_Archive/ volume the exact millisecond the data is committed to the agent's internal memory/database. Relying on read-state flags within the JSON itself is strictly forbidden.

RESPONSIVE MATRIX TEARDOWN PROTOCOL: All enterprise data tables exceeding 5 columns must natively implement a data-label CSS reflow matrix. Do not build parallel mobile components; forcefully dismantle the <table> into stacked, touch-friendly Key-Value cards on viewports < 900px.

MANDATORY REVIEW ISOLATION PROTOCOL: Destructive or state-advancing authorization actuations (e.g., Approve/Reject) are strictly forbidden as inline table elements. They must be isolated within a constrained Detail Modal to physically force the operator to ingest the contextual payload (e.g., Technician Logs) before executing the command.
